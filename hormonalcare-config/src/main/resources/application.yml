server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Fundamentos-3584-ninjacode-hormonalcare/microservicios
          search-paths: config
          clone-on-start: true
